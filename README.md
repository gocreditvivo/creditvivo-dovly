# creditvivo-dovly static Vercel build

This is a no-build standalone website for Vercel.

Use these Vercel settings:

- Framework Preset: Other
- Root Directory: ./
- Build Command: leave blank
- Output Directory: leave blank
- Install Command: leave blank

Files must be at the repo root, not inside another folder.
The homepage is index.html.
